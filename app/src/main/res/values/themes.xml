<resources>

    <style name="Theme.TickVision" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="android:colorAccent">@color/accentColor</item>
        <item name="colorButtonNormal">@color/primaryColor</item>
        <item name="mtf_backgroundColor">@color/primaryColor</item>
        <item name="mtf_labelColor">@color/disable</item>
    </style>

    <style name="fullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="checkBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/disable</item>
        <item name="colorControlActivated">@color/primaryColor</item>
    </style>

    <style name="animDialog">
        <item name="android:windowEnterAnimation">@anim/fade_in_dialog</item>
        <item name="android:windowExitAnimation">@anim/fade_out_dialog</item>
    </style>

    <style name="toolbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/primaryUltraLightColor</item>
    </style>

    <style name="floatingAction" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">@font/iran_sans</item>
        <item name="android:textSize">@dimen/numberTextSize</item>
        <item name="menu_labels_margin">10dp</item>
        <item name="android:textColor">@color/primaryColor</item>
    </style>
</resources>